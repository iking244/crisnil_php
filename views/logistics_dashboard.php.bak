<?php
include "../controllers/logistics_controller.php";
?>

<!DOCTYPE html>
<html lang="en">

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logistics Dashboard</title>
    <link rel="stylesheet" href="../styles/logistics.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="../styles/dashboard2.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="icon" href="../imgs/imgsroles/logocrisnil.png" type="image/x-icon">
</head>

<body>
    <?php include '../includes/header.php'; ?>
    <?php include '../includes/sidenav.php'; ?>
    <?php include '../includes/status_helper.php'; ?>


    <!-- Map Hero Section -->
    <section class="map-hero">

        <!-- Map container -->
        <div id="map"></div>

        <!-- Overlay stats -->
        <div class="map-stats">
            <h3>Logistics Overview</h3>
            <p>Active Routes: <?= $stats['active_routes'] ?></p>
            <p>Orders today: <?= $stats['orders_today'] ?></p>
            <p>Delayed: <?= $stats['delayed'] ?></p>
        </div>

    </section>


    <!-- Cards -->
    <section class="dashboard-cards">

        <div class="card blue">
            <h3>ORDERS</h3>
            <div class="card-body">

                <table class="orders-table">
                    <thead>
                        <tr>
                            <th>Job #</th>
                            <th>From</th>
                            <th>To</th>
                            <th>Status</th>
                            <th>ETA</th>
                        </tr>
                    </thead>
                    <tbody>

                        <?php if ($job_orders && $job_orders->num_rows > 0): ?>
                            <?php while ($job = $job_orders->fetch_assoc()): ?>
                                <tr>
                                    <td class="order-id">#<?= $job['id']; ?></td>
                                    <td><?= $job['origin']; ?></td>
                                    <td><?= $job['destination']; ?></td>
                                    <td>
                                        <?= renderStatusBadge($job['status']); ?>
                                    </td>
                                    <td>
                                        <?php
                                        if (!empty($job['eta'])) {
                                            echo date('H:i', strtotime($job['eta']));
                                        } else {
                                            echo "-";
                                        }
                                        ?>
                                    </td>
                                </tr>
                            <?php endwhile; ?>
                        <?php else: ?>
                            <tr>
                                <td colspan="5">No active job orders.</td>
                            </tr>
                        <?php endif; ?>

                    </tbody>
                </table>

                <div class="card-footer">
                    <button class="view-btn" type="button" 
							onclick="window.location.href='logistics_orders.php'">
						View All Orders
					</button>
                </div>

            </div>
        </div>



        <div class="card red">
            <h3>ROUTES</h3>
            <div class="card-body">

                <table class="orders-table">
                    <thead>
                        <tr>
                            <th>Trip #</th>
                            <th>Truck</th>
                            <th>Status</th>
                            <th>Departed</th>
                        </tr>
                    </thead>
                    <tbody>

                        <?php if ($active_trips && $active_trips->num_rows > 0): ?>
                            <?php while ($trip = $active_trips->fetch_assoc()): ?>
                                <tr>
                                    <td class="order-id">#<?= $trip['trip_id']; ?></td>
                                    <td><?= $trip['truck_plate_number']; ?></td>
                                    <td>
                                        <?= renderStatusBadge($trip['status']); ?>
                                    </td>
                                    <td>
                                        <?php
                                        if (!empty($trip['departure_time'])) {
                                            echo date('H:i', strtotime($trip['departure_time']));
                                        } else {
                                            echo "-";
                                        }
                                        ?>
                                    </td>
                                </tr>
                            <?php endwhile; ?>
                        <?php else: ?>
                            <tr>
                                <td colspan="4">No active routes.</td>
                            </tr>
                        <?php endif; ?>

                    </tbody>
                </table>

                <div class="card-footer">
                    <a href="trips.php" class="view-btn">View All Routes</a>
                </div>

            </div>
        </div>


        <div class="card green">
            <h3>UNSCHEDULED ORDERS</h3>
            <div class="card-body">

                <table class="orders-table">
                    <thead>
                        <tr>
                            <th>Job #</th>
                            <th>From</th>
                            <th>To</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>

                        <?php if ($unscheduled_jobs && $unscheduled_jobs->num_rows > 0): ?>
                            <?php while ($job = $unscheduled_jobs->fetch_assoc()): ?>
                                <tr>
                                    <td class="order-id">#<?= $job['id']; ?></td>
                                    <td><?= $job['origin']; ?></td>
                                    <td><?= $job['destination']; ?></td>
                                    <td>
                                        <?= renderStatusBadge($job['status']); ?>
                                    </td>
                                </tr>
                            <?php endwhile; ?>
                        <?php else: ?>
                            <tr>
                                <td colspan="4">No unscheduled orders.</td>
                            </tr>
                        <?php endif; ?>

                    </tbody>
                </table>

                <div class="card-footer">
                    <button class="view-btn">View All Orders</button>
                </div>

            </div>
        </div>


    </section>



    <script src="../scripts/logistics.js"></script>
    <script
        async
        src="https://maps.googleapis.com/maps/api/js?key=<?= $GOOGLE_MAPS_API_KEY ?>&callback=initMap"
        defer>
    </script>
    <script type="text/javascript" src="../scripts/notif.js"></script>
    <script type="text/javascript" src="../scripts/sidenav.js"></script>
    <script type="text/javascript" src="../scripts/dropdown2.js"></script>

</body>

</html>